# Copy this file to .env (in this same backend/ folder) and fill in your
# local MySQL credentials. .env is git-ignored — never commit real
# credentials.

# MySQL connection
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=brgydata

# Port the Express server (and the Electron window) listens on
PORT=3000
